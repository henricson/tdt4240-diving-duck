package components

import com.badlogic.gdx.math.Vector2
import com.divingduck.components.PositionComponent
import org.junit.Assert.*
import org.junit.Test

class PositionComponentTest{
    //Should be correct if velocity is moved to the position component??

    @Test
    fun `has right values`(){
        //val comp = PositionComponent(Vector2(30f, 10f), Vector2(30f,50f));

        //assertEquals(Vector2(30f,10f), comp.position);
        //assertEquals(Vector2(30f,50f),comp.velocity);
    }

}
